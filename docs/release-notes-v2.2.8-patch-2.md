# Enfyra App v2.2.8-patch-2

## Bug Fixes
- Fixed `app/composables/init/useNavigationActions.ts` to import `CommonBreadCrumbs` with the component name generated by Nuxt, restoring production builds that failed during component import resolution.
- Added direct `package.json` dependencies for `@codemirror/state`, `@tiptap/extension-link`, `@tiptap/extension-placeholder`, `@tiptap/extension-underline`, and `@tiptap/extensions` so local installs and generated app installs do not depend on hoisted or transitive packages.
- Updated dark theme variables in `app/assets/css/theme.css` to keep shell, canvas, and flow surfaces readable while preserving the active primary accent.
- Updated `package.json` version to `2.2.8-patch-2`.
